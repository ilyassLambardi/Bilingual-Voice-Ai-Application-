# ══════════════════════════════════════════════════════════
# Cloud-mode only dependencies (Groq API, no local models)
# ══════════════════════════════════════════════════════════

# ── Core ─────────────────────────────────────────────────
torch>=2.1.0
numpy>=1.24.0
python-dotenv>=1.0.0

# ── Server ───────────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# ── Cloud APIs ───────────────────────────────────────────
groq>=0.4.0

# ── TTS / VAD (lightweight, always local) ────────────────
edge-tts>=7.0.0
imageio-ffmpeg>=0.5.1
